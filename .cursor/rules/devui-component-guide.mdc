---
description: DevUI2 组件库使用指南 - 指导设计 agent 在什么场景下使用什么组件，以及组件之间的嵌套逻辑
globs: "**/*.pen"
alwaysApply: false
---

# DevUI2 组件库使用指南

组件库路径: `designsystem/devUI/components/devUI2.pen`

使用前必须将组件复制到目标 `.pen` 文件中（跨文件不能直接 ref 引用）。
组件名以 `.` 开头或带「/」变体描述的是**基础/子组件**，通常作为更高级组件的构成部分，按需选用具体状态或数量变体。

---

## 使用组件时必须填充文案（必读）

组件库中的组件自带的是**占位文案**（如「一级菜单」「页签一」「编号」「标题」）。用 ref 引用组件后，**必须**把其中所有需要展示的文本替换为业务文案，否则设计稿会保留占位、语义不完整。

- **如何填充**：在插入时使用 `descendants`，例如 `I(parent, { type: "ref", ref: "devui:组件ID", descendants: { "devui:文本节点ID": { content: "实际文案" }, ... } })`。文本节点 ID 需通过 `batch_get(..., resolveInstances=true)` 读取组件内部结构获得。
- **最容易遗漏的**：**顶栏**、**侧栏/工具链导航**、头信息、表格表头。这类组件内含多个文本节点，必须在当次插入时一次性在 descendants 中填齐，不得只填部分或事后用 `U()` 补（会污染组件定义）。
- **按组件类型**：下面各节中的按钮、页签、面包屑、标签、表格、卡片等，凡有「文案/标题/表头/菜单项」的，均需在实例化时按 Spec 或内容物料表填齐。详见 `pen-design-generator` Skill 中的「组件文案填充」与 `memory/pen-design-common-mistakes.md`。

---

## 1. 按钮 Button

### 场景与选择

| 场景 | 组件 | 推荐 ID（default 状态） |
|------|------|------------------------|
| 主操作（提交、确认、新建） | 按钮-主要 `tSwIg` | `mYPZF`(md) `Rl5jm`(sm) `UtovU`(lg) |
| 辅助操作（取消、返回、编辑） | 按钮-次要 `ChE0Z` | `VKSqi`(md) `jxhLP`(sm) `rTL5w`(lg) |
| 带图标的操作按钮 | 按钮-图标文本按钮 `Krfl3` | `6LFBN`(md) `3i79O`(sm) |
| 带下拉菜单的按钮（如「更多操作」） | 按钮-可下拉次要组合按钮 `rrLTG` | `ziNQG`(icon/md) `r9lnG`(icon/sm) `rJDJL`(number/md) `gZrTy`(number/sm) |

**尺寸规则**: lg 用于页面主操作区，md 用于表单/卡片内，sm 用于表格行内或紧凑空间。
**状态**: 每个按钮都有 default/hover/active/disabled 四种状态，设计稿中一般使用 default 状态。

### 嵌套逻辑

按钮是原子组件，内部由文本 + 可选图标构成。可下拉组合按钮 = 按钮-次要 + 分隔线 + 下拉图标区域。

---

## 2. 页签 Tabs

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 胶囊式切换（紧凑，适合筛选） | 页签Pills `5AtIy` | `l9A3O`(2项) `Z70QE`(3项) `KEOhA`(4项) `6YelH`(5项) `8L4up`(6项) |

### 嵌套逻辑

页签组 = 页签/number 变体组合，名称格式为「页签/number=2」～「页签/number=6」。插入后需用 descendants 填齐每个页签的标签文案。

---

## 3. 面包屑 BreadCrumbs

### 场景

用于页面层级导航，展示当前位置路径。

### 选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 标准面包屑（2～6 层） | 面包屑组 `AlQDE` | `ydEdG`(2层) `UOL0h`(3层) `yl4Dd`(4层) `EF14Z`(5层) `oNW38`(6层) |
| 省略形式面包屑 | 面包屑组 | `H50rT`(ellipsis) |

### 嵌套逻辑

面包屑组内为「面包屑/面包屑个数-number=2」～「6」及「ellipsis」变体。插入时在 descendants 中按层级填齐各段文案。

---

## 4. 标签 Tag

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 状态标签（成功/运行中） | 标签 | `jezfD` |
| 辅助信息标签 | 辅助标签 | `qyFas` |
| 通用标签（分类/标记） | 常规标签 | `S4vFN` |
| 线性轮廓标签 | 线性标签 | `9iCpY` |

**颜色规则**: 按组件内变体区分 green=成功/正常，orange=警告/进行中，red=错误/危险，grey=默认/禁用。

---

## 5. 搜索框 Search

### 场景

页面或表格上方的搜索输入区域。

### 选择

搜索框-线框白底风格 `sLVmB`，按图标位置和状态选择：
- 左侧搜索图标: `pClmN`(default-noContent) `8wlP5`(hover) `8440P`(activation-inputPending) `3oslM`(activation-Entered) `Yvn9i`(contentpresent) `kUBVi`(disabled)
- 右侧搜索图标: `sMfy0`(default-noContent) `eMmaO`(hover) `fFllq`(activation-inputPending) `bE46t`(activation-Entered) `WwjQs`(contentpresent) `ljKNe`(disabled)

推荐默认使用**左侧图标 + default-noContent** 状态 (`pClmN`)。

---

## 6. 复选框 Checkbox

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 带文本的复选框 | 复选框文本组合 | `kR6rb` |

### 嵌套逻辑

复选框文本组合 = 复选框 + 文本标签。纯复选框在「复选框 Checkbox」帧内，可按需 batch_get 获取未选/已选/半选状态子组件。

---

## 7. 分页器 Pagination

### 场景

用于数据列表底部的翻页控制。

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 分页组件 | 分页 Pagination | `7a2rV` |

### 嵌套逻辑

分页 Pagination 内为页码、翻页箭头等子组件，具体变体可通过 batch_get 在 `8Myhi` 下查看。

---

## 8. 输入框 TextInput

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 标准文本输入 | 文本输入框 `zUrj8` | `rn9tI`(noContent) `tdhOO`(hasContent) `uF43C`(hover) `OLGBd`(focus) `yF2iu`(input) `QgeQz`(danger) `Um9VZ`(success) `ZnH43`(disabled) |
| 数字输入 | 数字输入框 `FPTv3` | `ko1Kw`(default) `hxtGb`(hover) |
| 带标题的表单输入项 | 表单项-多行文本输入框 | `VFC67` 内 `qjjWq`；表单项-文本输入框需在「输入框 TextInput」帧内组合 |
| 表单标题 | 表单标题 `mySBY` | `7iMMv`(基础) `67xGb`(提示图标) `78DAm`(必填) `kzZ0d`(必填+提示) |

### 嵌套逻辑（重要）

**表单项-文本输入框** 由表单标题 + 文本输入框 + 可选辅助文本组成。选择时需确定：
- `状态-status`: noContent/hasContent/hover/focus/input/danger/success/disabled
- `必选图标-required` / `提示图标-helpTips` / `是否展示辅助文本-showExtraInfo`

---

## 9. 选择框 Select

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 单选下拉框（未选/关） | 下拉选择框 `zlQjV` | `zTtRm`(default/未选/关) `g8ll6`(default/已选/关) |
| 带展开面板的下拉框 | 下拉选择框 | `LjSx5`(未选/开) `zvNlm`(已选/开) |
| 其他状态 | 下拉选择框 | `oEm0T`(hover/未选) `V8Ggo`(hover/已选) `JTmP2`(select/未选) `OX3vH`(select/已选) `GK2ZR`(active/已选) `JRnyz`(disabled) |

### 嵌套逻辑

下拉选择框（触发器）点击后展开基础选择面板，内为下拉选项。表单项-下拉选择框在「表单 form」中组合使用。

---

## 10. 表格 DataTable

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 单线表格（有阴影） | 表格 `xz7LI` | `8K8z3`(singleLine + shadow) |
| 单线表格（无阴影） | 表格 | `BUBhb`(singleLine + no shadow) |
| 带分隔线表格（有阴影） | 表格 | `8Oq0P`(tableWithDividers + shadow) |
| 带分隔线表格（无阴影） | 表格 | `2PPub`(tableWithDividers + no shadow) |

### 嵌套逻辑

表格 = 表头行 + 表体行。表头/表体单项按 position、contentType、灰色背景等变体区分。插入后**必须**在 descendants 中填齐表头列名与关键单元格文案，不能留「编号」「标题」等占位。

---

## 11. 筛选 Filter

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 简易筛选（单条件） | 筛选 `QcTiH` | `Jz8Tn`(default) `kasjZ`(hover) `IBiU4`(active) |
| 复杂筛选（多条件） | 筛选 | `3Nppx`(default) `xwumW`(hover) `ImL7Y`(active) |

### 嵌套逻辑

筛选 = 筛选触发器 + 展开后面板。类型由「筛选/状态-status=..., 类型-type=简易|复杂」区分。

---

## 12. 分类搜索 CategorySearch

### 场景

带分类标签的搜索框，支持按类别过滤搜索。

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 默认状态（标签关） | 分类搜索 `ICHE2` | `TnjT0`(default/tag=关) |
| 默认状态（标签开） | 分类搜索 | `Egl1a`(default/tag=开) |
| 输入/悬停状态 | 分类搜索 | `YZlVY`(hover/tag=关) `FH7dj`(hover/tag=开) |

---

## 13. 表单 Form

### 场景

完整的表单组件，带标题、输入控件和可选按钮。

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 下拉选择表单项（带按钮） | 表单组件 `eL2HR` | `lXYgx`(showExtraInfo=false) `ziusM`(showExtraInfo=true) |
| 下拉选择表单项（无按钮） | 表单组件 | `Crm3Z` `H2LJA` |
| 输入框表单项（带按钮） | 表单组件 | `QzXy7`(showExtraInfo=false) `AEMdH`(showExtraInfo=true) |
| 输入框表单项（无按钮） | 表单组件 | `Zk0Ev` `XI7d1` |

---

## 14. 卡片 Cards

### 场景与选择

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 项目卡片（带项目图标和信息） | 项目卡片 `a74K6` | `9qbEz`(默认) `PyyzD`(悬浮) `0sBFU`(悬浮-标题变蓝) |
| 基本卡片（通用信息展示） | 基本卡片 `QnsHG` | `MBNRG`(默认) `8vipZ`(悬浮) `veIiD`(悬浮-标题变蓝) |
| 带图卡片（顶部有图片区域） | 带图卡片 `RKPRI` | `HKJpB`(默认) `Z9pnT`(悬浮) `UXGvV`(悬浮-标题变蓝) |
| 现网卡片样式 | 现网卡片样式 `cpmQU` | `JIqzC`(default) `eJVv6`(hover) |

### 嵌套逻辑

卡片内为标题区、信息展示、标签区等。用于数据展示时需在 descendants 中填齐标题、描述、数据与操作文案。

---

## 15. 业务组件（更多卡片）

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 左侧选择菜单卡片 | 左侧选择菜单卡片 | `VP3uY` |
| Banner（大横幅区域） | banner | `Zj0tz` |
| 公告卡片 | 公告卡片 | `FvbbS` |
| 活动卡片 | 活动卡片 | `gk3XY` |
| 帮助文档卡片 | 帮助文档卡片 | `x0QDF` |
| 字母图标（用于项目标识） | 字母icon | `N8QxK` |

---

## 16. 头信息 Header

### 场景

页面顶部的头信息区域，含标题面包屑、搜索和操作按钮。

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 默认头信息 | 头信息 `hJlzm` | `TUWxd`(default) |
| 带搜索的头信息 | 头信息 | `ZU5hh`(search/2项) `ju9uI`(3项) `v7KDW`(4项) |

### 嵌套逻辑

头信息 = 头信息标题面包屑组 + 操作按钮区 + 可选搜索区域。名称格式为「头信息/状态=default|search, 搜索项数量-number=...」。插入时须一次性在 descendants 中填齐标题、返回、辅助信息等文案。

---

## 17. 侧边栏-工具链专用导航

### 场景

工具链平台的左侧导航栏。

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 收起状态的图标导航 | 工具链专用导航 `bdJpP` | `SQOb3`(展开=off) |
| 展开状态的完整导航 | 工具链专用导航 | `E36oP`(展开=on) |

### 嵌套逻辑

工具链专用导航内为选择项目、一级/二级菜单、热区按钮等。**极易遗漏文案**，插入时必须带齐 descendants 中的项目名、一级/二级菜单项。

---

## 18. 侧边导航-手风琴

### 场景

通用的手风琴式侧边导航栏，适用于多级菜单。

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 完整侧边栏 | 侧边栏 | `K0pVg` |

### 嵌套逻辑

侧边导航-手风琴帧 `mGA8w` 内为可复用侧边栏 `K0pVg`，内含一级菜单组及二级菜单项，需填齐菜单文案。

---

## 19. 顶部导航栏

### 场景

全局顶部导航栏。

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 默认顶部导航栏 | 顶部导航栏 `etDDY` | `Uexy2`(种类=default) |
| 占位顶部导航栏 | 顶部导航栏 | `2bjDj`(种类=占位) |

### 嵌套逻辑

顶部导航栏含 Logo/品牌区、菜单组、定位、菜单按钮、右侧图标、头像等。**必须**在插入时一次性在 descendants 中填齐产品名、菜单项、区域、用户等多文本节点。

---

## 20. 链接 Link

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 默认链接 | 链接 Link `qcyuo` | `FfVdW` |
| 表格内链接 | 表格文字黑色链接 / 表格加粗列文字链接 | `l6AL7` `evKGv` `CbM2K` |

---

## 21. 滚动条、弹窗、步骤条、进度条、徽标等

| 场景 | 组件 | 推荐 ID |
|------|------|--------|
| 滚动条 | 滚动条 Scrollbar `sixna` | `C1bL7` |
| 模态弹窗 | 模态弹窗 Modal `5nvBT` | `O8FNo`（基础类弹窗）；子组件 `xbwXy`(非模态) `WJxDN`(模态) |
| 步骤条 | 步骤条-TaskStep `z20Fm` | `Zavv1`(横向-左右布局) `qPPvF`(横向-居中对齐) `FYkld`(纵向) |
| 进度条 | 进度条 Progress `VqXzE` | `bMfa8`(条形) `QqPAN`(circle) |
| 徽标 Badge | 徽标 Badge `SLd8X` | `wSZml` |
| 图片上传 | 图片上传 ImageUpload `4Ko6M` | `cA3sd` |
| 级联菜单 | 级联菜单 Cascader `0AW94` | `fXw1o` |
| 缺省页插画 | 缺省页插画 `BSQxu` | `UksPg`(缺省图) `aZADr`(小尺寸卡片插图) |

---

## 组件组合最佳实践

### 典型页面布局

```
顶部导航栏 (`Uexy2` / `etDDY`)
├── 侧边栏导航（工具链导航 `E36oP`/`SQOb3` 或 手风琴 `K0pVg`）
└── 主内容区
    ├── 头信息 (`TUWxd` / `hJlzm`)
    ├── 筛选/搜索区（筛选 + 搜索框 `pClmN` + 按钮）
    ├── 页签切换（页签Pills `l9A3O`～`8L4up`）
    ├── 数据表格 (`8K8z3`/`BUBhb`/`8Oq0P`/`2PPub`) + 分页器 (`7a2rV`)
    └── 或 卡片网格（基本卡片/项目卡片）
```

### 表单页面

```
头信息
└── 表单区域（表单组件 `eL2HR` 内选对应变体）
    ├── 表单项-文本输入框（结合 表单标题 + 文本输入框）
    ├── 表单项-下拉选择框
    ├── 复选框文本组合 (`kR6rb`)
    └── 按钮组（按钮-主要 `mYPZF` + 按钮-次要 `VKSqi`）
```

### 组件文案填充速查（易遗漏）

| 组件类型 | 需填充内容 | 说明 |
|----------|------------|------|
| **顶栏** `etDDY`/`Uexy2` | 产品名、菜单项、区域、用户等 | 多文本节点，必须一次性在 descendants 中填齐 |
| **侧栏/工具链导航** `E36oP`、`K0pVg` | 项目名、一级/二级菜单项 | 极易遗漏，插入时必须带齐 descendants |
| **面包屑** `AlQDE` 等 | 各层级路径文案 | 2～6 层按实际层级填 |
| **头信息** `hJlzm` | 标题、返回按钮、辅助信息、详情行 | 与页面业务强相关 |
| **页签** `5AtIy` 等 | 每个页签的标签文字 | 与当前页功能一致 |
| **按钮** `tSwIg`、`ChE0Z` 等 | 按钮上的文字 | 如「返回」「提交」「取消」 |
| **表格** `xz7LI` 等 | 表头列名、关键单元格 | 表头必须为业务列名，不能留「编号」「标题」等 |
| **卡片** `a74K6`、`QnsHG` 等 | 标题、描述、数据、操作文字 | 用于数据展示时需填具体指标与说明 |
| **搜索框** `sLVmB`/`pClmN` | 占位提示文案 | 如「请输入关键词」 |
| **标签** `jezfD` 等 | 标签文字 | 如「进行中」「已通过」 |
