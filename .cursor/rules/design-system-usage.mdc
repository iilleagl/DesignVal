---
description: 设计系统使用指引——组件优先、规范遵从
globs: "**/*.pen, designsystem/**"
alwaysApply: false
---

# 设计系统使用指引

## 1. 组件优先

- **必须**优先用设计系统/组件库中的组件满足用户意图。
- 有现成组件时，用 **ref 引用 + 改文案/属性**（如 `descendants`、`U(实例ID/组件子节点ID, {...})`），不要用基础 frame/text 手搭整块。
- 示例：顶栏、侧栏、表格、按钮、搜索框、分页等，均优先从 `designsystem/devUI/components/devUI2.pen` 选用并实例化。

## 2. 组件中的文案必须填充（必做）

- 组件库里的组件自带的是**占位文案**（如「一级菜单」「二级菜单」「编号」「标题」）。**凡通过 ref 引用的组件，只要内含可读文案，都必须按业务需求替换为实际文案**，否则设计稿语义不完整、无法交付。
- **需要填充的典型组件**：顶栏、**侧栏/工具链导航**、面包屑、头信息、页签、按钮、表格（表头与关键单元格）、卡片（标题/描述/数据）、搜索框占位符、标签等。其中顶栏、侧栏、头信息、表格为**多文案组件**，内含多个文本节点，最容易遗漏。
- **正确做法**：插入 ref 时在 **当次** `I(..., { ref, descendants: { "devui:文本节点ID": { content: "实际文案" }, ... } })` 中**一次性填齐**该组件内所有需展示的文案。先用 `batch_get(..., resolveInstances=true)` 读出组件内所有 text 节点 ID，再写入 descendants。禁止插入多文案组件时不带 descendants 或只填部分（如只改顶栏不改侧栏）。
- **错误后果**：若首次未带齐 descendants，后续用 `U(实例ID/devui:子节点ID)` 会污染组件定义；漏填的侧栏/顶栏会整块保留「一级菜单」「二级菜单」等占位。详见 `.cursor/skills/pen-design-generator/SKILL.md` 中「组件文案填充」与 `memory/pen-design-common-mistakes.md` 第 4、9 条。

## 3. 组件无法满足时再扩展

- 仅当组件库**没有**可匹配的组件或**无法通过配置/文案满足**需求时，才：
  - 在现有组件上做**修改**（变体），或
  - **设计新组件**。
- 新组件或变体仍须符合设计规范（颜色、字号、间距、圆角等），并尽量复用 Design Token。

## 4. 符合设计规范文档

- 设计输出必须符合项目中的**设计规范文档**（如 `designsystem/devUI/llms-full.txt` 及技能中的 reference）。
- 使用规范中的：颜色 Token、字号与行高、间距体系、圆角与阴影、组件规格；禁止随意写死与规范冲突的色值或尺寸。
